<%*
let docs = await tp.system.prompt("문서 이름들을 쉼표(,)로 구분해서 입력 (예: 중앙집권화, 귀족정, 철인정치):");
let docArray = docs.split(",").map(s => s.trim()).filter(s => s.length > 0);
let result = "";

for (let doc of docArray) {
    result += `<span style="background-color: var(--background-secondary); padding: 2px 8px; border-radius: 4px; margin-right: 5px;"><a class="internal-link" data-href="${doc}" href="${doc}">${doc}</a></span> `;
}
tR += result;
_%>